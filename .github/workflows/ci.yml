name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create build report
        run: echo "Build completed for Python ${{ matrix.python-version }}" > build-report.txt

      - name: Upload build report
        uses: actions/upload-artifact@v4
        with:
          name: build-report-${{ matrix.python-version }}
          path: build-report.txt

  test:
    runs-on: ubuntu-latest
    needs: build
    env:
      MY_API_TOKEN: ${{ secrets.MY_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download build report
        uses: actions/download-artifact@v4
        with:
          name: build-report-3.11

      - name: Show build report
        run: cat build-report.txt

      - name: Run tests
        run: pytest

      - name: Notify on main only
        if: github.ref == 'refs/heads/main'
        run: echo "This step only runs when pushing to main."
